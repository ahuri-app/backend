generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_FILE_PATH")
}

model User {
  id       String @id @default(cuid())
  eid      String
  email    String @unique
  username String
  tag      String @unique
  password String
}
